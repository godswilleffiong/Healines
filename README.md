# Healines

## Installation

#### `Step 1` - clone the repo
  
#### `Step 2` - cd in the repo

#### `Step 3` - install dependencies

```bash
$ npm install
```
#### `Step 4` - run application

```bash
$ npm run start
```
